import styled from "styled-components"

export const LinkWithPopover = styled.div`
  display: flex;
  justify-content: space-between;

  & > *:last-child {
    position: relative;
    top: -4px;
  }
`
