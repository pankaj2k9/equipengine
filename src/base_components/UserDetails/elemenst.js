import styled from "styled-components"

import Button from "base_components/RootButton"

export const ButtonSetPassword = Button.extend`
  width: inherit;
  padding: 10px;
`

export const Spacing = styled.span`
  margin-right: 0.7em;
`
