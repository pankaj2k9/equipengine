import PropTypes from 'prop-types'

/**
 * -------------------------------------
 * PropTypes and defaultProps for AdminOrganisationSettings
 * -------------------------------------
 */
export default {
  props: {
    match: PropTypes.object.isRequired
  }
}
