import PropTypes from "prop-types"

export const contextPropTypes = {
  discussions: PropTypes.array,
  latestDiscussions: PropTypes.array,
  thread: PropTypes.object,
  selectThread: PropTypes.func
}
